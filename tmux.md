```bash
tmux ls      # list tmux sessions
tmux attach-session -t <session-name>  # connect to session
Cntr+b d     # detach from current session
tmux rename-session -t 0 docker        # rename session, here rename from '0' to 'docker'
```
